# Exercise 8 - Form Controls

Form đặt vé máy bay có state và hai sơ đồ bố cục theo đề.

- Mẫu mặc định: `http://localhost:3000/`
- Mẫu lưới 12 cột: `http://localhost:3000/?view=1`

```bash
npm install
npm start
```
